# Mobile app configuration — copy to .env and adjust.
# --------------------------------------------------

# Base URL of the backend API (no trailing slash, includes /api/v1)
EXPO_PUBLIC_API_URL="http://localhost:4000/api/v1"
