services:
  # Backend API
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: css-backend
    restart: unless-stopped
    ports:
      - "${PORT:-4000}:4000"
    env_file:
      - apps/backend/.env
